<% content_for :title, t('.title') %>

<%= render 'admin/shared/to_new_button', model_name: Area.model_name.human, path: new_admin_area_path %>
<div class="overflow-x-auto">
  <table class="table">
    <thead>
      <tr>
        <th><%= Area.human_attribute_name(:id) %></th>
        <th><%= Prefecture.human_attribute_name(:name) %></th>
        <th><%= Area.human_attribute_name(:name) %></th>
      </tr>
    </thead>
    <tbody>
      <%= render @areas %>
    </tbody>
  </table>
</div>
<%= render 'admin/shared/pagination', object: @areas %>