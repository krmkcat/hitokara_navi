<%= render 'admin/shared/to_new_button', model_name: Tag.model_name.human, path: new_admin_tag_path %>
<div class="overflow-x-auto">
  <table class="table">
    <thead>
      <tr>
        <th><%= Tag.human_attribute_name(:id) %></th>
        <th><%= Tag.human_attribute_name(:name) %></th>
      </tr>
    </thead>
    <tbody>
      <%= render @tags %>
    </tbody>
  </table>
</div>