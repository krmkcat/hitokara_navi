<div>
  <h1 class="font-bold text-2xl text-neutral text-center">
    <%= t('.title') %>
  </h1>
</div>

<% if @reviews.present? %>
  <%= render partial: 'my_reviews/review', collection: @reviews %>
<% else %>
  <p><%= t '.no_reviews' %></p>
<% end %>

<div class="text-center mt-4">
  <%= link_to t('.back_to_mypage'), mypage_path, class: 'btn btn-primary' %>
</div>
