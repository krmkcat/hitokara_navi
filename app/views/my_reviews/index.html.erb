<div>
  <h1 class="font-bold text-4xl">
    <%= t('.title') %>
  </h1>
  <div class="text-right mt-4">
    <%= link_to t('.back_to_mypage'), mypage_path, class: 'btn' %>
  </div>
</div>

<% if @reviews.present? %>
  <%= render partial: 'my_reviews/review', collection: @reviews %>
<% else %>
  <p><%= t '.no_reviews' %></p>
<% end %>

<div class="text-center mt-4">
  <%= link_to t('.back_to_mypage'), mypage_path, class: 'btn' %>
</div>
